import { Tabs as TabsRoot, TabsContent, TabsList, TabsTrigger } from '../../shadcn-primitives/tabs'

export const Tabs = Object.assign(TabsRoot, {
  Content: TabsContent,
  List: TabsList,
  Trigger: TabsTrigger,
})
